
modLoader="javafml" #mandatory

loaderVersion="[31,)" #mandatory This is typically bumped every Minecraft version by Forge. See our download page for lists of versions.

issueTrackerURL="https://github.com/smored/RefinedMetalSmelting" #optional

[[mods]] #mandatory

modId="rms" #mandatory

version="1.16.1-1.0.1" #mandatory

displayName="Refined Metal Smelting" #mandatory

updateJSONURL="https://github.com/smored/RefinedMetalSmelting" #optional

displayURL="https://github.com/smored/RefinedMetalSmelting" #optional

logoFile="rmslogo.png" #optional

credits="smore_d, NitroPan, floh22" #optional

authors="smore_d" #optional

description='''
Adds various tiers of metal smelting that yields respective tiers of metal purities, utilizing both realistic and fantasy elements.
'''

[[dependencies.examplemod]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[31,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"

[[dependencies.examplemod]]
    modId="minecraft"
    mandatory=true
    versionRange="[1.15.2]"
    ordering="NONE"
    side="BOTH"
